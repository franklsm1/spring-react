defaultTasks 'buildFrontend',
        'backend:clean',
        'backend:build'

task buildFrontend {
    dependsOn 'frontend:yarn_test-build-jar'
}

task startFrontend {
    dependsOn 'frontend:yarn_test-start'
}

task wrapper(type: Wrapper) {
    gradleVersion = '4.6'
}